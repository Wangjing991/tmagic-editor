/**
 * 组件类型枚举，必须唯一
 */
export enum ComponentType {
  Page = 'page',
  Container = 'container',
  Overlay = 'overlay',
  Text = 'text',
  Button = 'button',
  Image = 'img',
  Qrcode = 'qrcode',
  Empty = 'empty',
}
